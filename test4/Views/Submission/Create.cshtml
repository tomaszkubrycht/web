@model test4.Models.Submission

<form asp-action="Create">
    <fieldset class="idea-form">
        <h2>Submit an Idea</h2>
        <div id="record">@Html.TextAreaFor(m => m.Content)
        @Html.TextAreaFor(m => m.nrid)</div>
        <input type="submit" name="Create" />
    </fieldset>
</form>